# ambreenqureshi.com — Ambreen Qureshi, personal brand

Sister website of saadwaqas.com (same "Midnight Keynote" design family), completely
isolated: own repository, own Vercel project, own domain, own analytics.

- **Who**: Ambreen Qureshi — Founder & Managing Director, Amber Homes Real Estate (Dubai, ORN 18690)
- **Stack**: static HTML generated by `scripts/gen-pages.mjs` into `site/`, one Vercel
  serverless function (`api/contact.js`), deployed on Vercel (`outputDirectory: site`)
- **Fonts**: Cormorant Garamond SemiBold (headings) + Manrope (body/UI)
- **Tokens**: canvas `#0D111A` · panel `#12161F` · text `#F0F2F5` · muted `#8A93A6`
  · bronze `#D99B62` / `#E6B27F` / `#B97843` · border `#283143`

## Build & QA
```
npm run check     # gen-pages + QA crawler (1 H1, titles, canonicals, JSON-LD, links, alts)
```

## Deploy
```
npx vercel --prod # personal scope, project: ambreenqureshi-com — NEVER the CRM team
```
DNS (GoDaddy): `A @ → 216.198.79.1` (or 76.76.21.21) · `CNAME www → cname.vercel-dns.com`.

## Facts discipline
Every public claim maps to /evidence. Company awards belong to Amber Homes Real Estate.
USD 1.5B+ is an internally reported cumulative figure and is always labelled as such.
RAKBANK "She Means Business" is a campaign feature — never call it an award/nomination.
Do not touch: saadwaqas.com, amberhomes.ae, salescenter.ae, AmberCRM (crm.amberhomes.ai).

## Pending inputs (owner)
- RESEND_API_KEY (+ optional CONTACT_TO/CC/FROM) in Vercel → activates direct email
  delivery from the contact form (until then the form offers the WhatsApp fallback).
- GA4 Measurement ID → set `SITE.gaId` in scripts/site-data.mjs and rebuild.
- RAKBANK feature link/asset → add to /media (MEDIA[0].href in scripts/site-data.mjs).
- 2022 award photograph → drop into site/assets/img/awards/ and wire in site-data.
